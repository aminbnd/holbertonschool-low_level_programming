#include <stdio.h>
/**
 *main - Entry point
 *Return: 0
 */
int main(void)
{
	int a = 0;
	int b = 1;

	do {
		putchar ('0' + a / 10);
		a++;
		if (b > a)
		putchar('0' + b % 10);
		b++;
	} while (a < 100 || b < 100);
	putchar ('\n');
	return (0);
}
